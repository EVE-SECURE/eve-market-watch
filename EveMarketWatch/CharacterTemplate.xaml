<UserControl x:Class="com.zanthra.emw.CharacterTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="82" d:DesignWidth="64">
    <Grid Height="80" Width="64">
        <Grid.RowDefinitions>
            <RowDefinition Height="64*" />
            <RowDefinition Height="16*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Path=name}" VerticalAlignment="Top" Grid.Row="1" />
        <Image Source="{Binding Path=imageUrl}" Name="image1" />
    </Grid>
</UserControl>
